name: tailscale

services:
  tsdproxy:
    image: almeidapaulopt/tsdproxy:1
    restart: unless-stopped
    volumes:
      - data:/data
      - ./:/config
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "8888:8888"
    networks:
      - tailscale

volumes:
  data:

networks:
  tailscale:
    driver: bridge
    name: tailscale
